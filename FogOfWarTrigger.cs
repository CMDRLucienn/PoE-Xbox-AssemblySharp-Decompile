using UnityEngine;

public class FogOfWarTrigger : MonoBehaviour
{
	[Tooltip("If true, the trigger must be 100% revealed before triggering (faded values will not trigger).")]
	public bool FullyRevealed;
}
